{
  "name": "lemon-qualifier",
  "private": true,
  "version": "2.0.0",
  "description": "Lemon Law Intake Qualifier — public landing page + per-firm gated demo",
  "scripts": {
    "build": "node build.js",
    "test": "node test/engine.test.js && node test/rules.test.js"
  }
}
